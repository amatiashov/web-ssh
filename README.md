```bash
export WEB_SSH_DOMAIN=domain.name
export WEB_SSH_DOMAIN_TOKEN=domain.token
git clone https://github.com/amatiashov/web-ssh.git && cd web-ssh && ./run.sh
```

```bash
export WEB_SSH_DOMAIN=domain.name
export WEB_SSH_DOMAIN_TOKEN=domain.token
apt update && apt install -y git && git clone https://github.com/amatiashov/web-ssh.git && cd web-ssh && ./run.sh
```

https://github.com/nirui/sshwifty